describe("ProductItems.js", () => {
  test("Prevents app from crashing if required props are not passed", () => {});
  test("Doesn't render if doesn't have the required props", () => {});
  test("Renders something even if there's just one product", () => {});
});
